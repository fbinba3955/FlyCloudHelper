# FlyCloudHelper 项目说明

FlyCloudHelper 是面向 Flymby 系列客户端的可扩展云端媒体扫描与目录服务。服务通过 Docker 部署，统一使用用户名和密码注册、登录，用户名和密码都至少 4 个字符。APP 添加网盘服务并开启“云端模式”后，需要输入云端地址；已有账号直接输入用户名和密码，没有账号则选择“新建账户”。登录成功后可以选择已有云端服务，也可以把 APP 当前配置的网盘连接、扫描路径、媒体类型和刮削配置创建为新的云端服务。FlyCloudHelper 持久管理这些服务配置，完成视频、音乐、有声书的扫描、识别、刮削、目录持久化、实时进度推送和多端查询。

一个 FlyCloudHelper 实例可以同时服务多个用户，每个用户可以创建多个云端服务。所有服务、扫描任务、媒体库和媒体条目直接归属于用户 ID。服务端从登录态确定 `userId`，为每个云端服务生成稳定的 `serviceId`，再创建其专属 `libraryId`；APP 的 `clientServiceId` 只作为某台设备上的本地服务绑定别名，不决定云端数据归属。用户在新设备上输入云端地址、用户名和密码后即可列出本人已有服务并选择同一 `serviceId`，从而读取相同扫描结果。不同用户和不同 `serviceId` 始终隔离，即使网盘类型、连接地址、账号或扫描路径完全相同也不会自动合并。

服务采用可安装的网盘 Provider 适配器和按媒体类型拆分的处理器，首批支持 WebDAV、光鸭、阿里云盘、百度网盘，后续增加网盘时不修改扫描核心和通用目录协议。云端服务的网盘密码、Token 或 Cookie 由独立凭据库长期加密保存，只能供扫描 Worker 使用，查询接口、日志、备份和管理页面均不得回显。音乐元数据处理参考 FlymbyServer 已有的多来源并发聚合、统一候选字段、来源故障隔离和可选 Chromaprint/AcoustID 声纹识别方法，但实现迁入 FlyCloudHelper Worker，不默认依赖 FlymbyServer 在线接口。

服务不代理、转码或存储媒体流。HarmonyOS APP 和 Android TV 从服务端读取媒体目录及网盘文件定位信息，再使用设备本地完成的网盘授权解析播放地址并直接播放；服务端保存的网盘凭据永不下发给客户端。新设备即使已经登录云端账号，仍需完成对应网盘的本地播放授权，否则只能浏览目录，不能播放。

内置 TMDB 刮削源由超级管理员在“系统配置”页面维护多个 Key，服务使用凭据主密钥将完整 Key 列表加密后写入数据库，读取接口和页面只返回数量、健康状态、并发和配置修订。保存配置后立即替换运行中的 Key 池，Key 池按最少在途请求调度；TMDB 实际刮削并发根据健康 Key 数量动态计算，并受单 Key 并发数和全局上限共同限制，某个 Key 被限流或失效时只降低该 Key 的容量。Key 不进入 APP、日志、任务快照或备份，也不在管理页面中回显。服务首次使用且尚未完成初始化时进入 `/setup` 向导，由部署者现场设置超级管理员用户名、密码和确认密码，用户名和密码仍至少 4 个字符；不要求一次性初始化凭证，也不通过环境变量预置账号密码。未显式提供凭据主密钥时，服务会自动生成并持久化，管理员创建成功后必须复制或下载备份并确认，确认后页面不再返回密钥原文。正常运行后，`/login` 为统一登录入口，`/register` 为普通用户注册入口，普通用户登录后进入 `/app` 管理本人云端服务、扫描任务和媒体目录；`super_admin` 超级管理员进入 `/admin`，可管理全部用户、云端服务、插件、任务和系统配置。公开注册只能创建普通 `user`，不能自行注册为超级管理员；后续超级管理员只能由现有超级管理员通过受保护且有审计的操作授予。Web 前端提供个人总海报墙、超级管理员全局海报墙，以及每个云端服务独立的海报墙，展示已入库的视频、音乐和有声书，但不提供播放器、播放按钮或可直接使用的媒体地址；首期也不允许通过前台上传或执行 Provider 适配器及其他任意代码插件。

数据库未配置时默认使用 SQLite，并将数据库文件保存到持久卷。部署者可通过 `FLYCLOUDHELPER_DATABASE_TYPE=postgres` 或 `mysql` 切换到 PostgreSQL/MySQL；既可分别配置地址、端口、数据库名、用户名和密码，也继续兼容完整连接 URL 和 Docker Secret。目标数据库不存在时默认使用同一账号尝试创建，再初始化 FlyCloudHelper 表结构；PostgreSQL 需要 `CREATEDB` 权限，MySQL 需要 `CREATE` 权限。切换数据库类型不会自动迁移原数据库中的管理员、用户、服务、任务、目录和插件数据。自动生成的主密钥独立保存在持久卷的 `secrets` 目录，单纯切换数据库不会删除它；删除数据卷或密钥文件才会造成密钥丢失。

## 当前阶段

项目已经完成首期后台代码闭环。Web 前端以 `Azure Brilliance Design` 原型作为视觉基准，技术栈为 Node.js 20、TypeScript、React、Vite、TanStack Router 和 Tailwind CSS；后台采用 Fastify、Knex 和内置扫描 Worker。`web/` 的初始化、登录、注册、概览、服务创建与配置、任务、媒体库、用户、插件、系统状态和审计页面均读取真实 API，任务页面使用 SSE 接收进度并保留轮询断线补偿，用户端和管理端均提供单服务海报墙。当前服务数据类型只开放“影视”；创建页展示“音乐”和“有声书”作为后续扩展项，但暂不可选。

后台已接入 SQLite/PostgreSQL/MySQL 配置层、跨数据库 schema、首次初始化事务、Argon2id、Cookie/APP Token、用户级数据隔离、加密网盘凭据、服务与配置修订、四类 Provider、任务队列与 SSE、影视扫描与目录、TMDB 多 Key、APP 文件定位、快照导出、超级管理员管理接口和审计。数据库、通用媒体模型和插件契约保留音乐与有声书扩展点，但当前 Worker 与能力接口只开放影视。生产构建由同一 API 进程提供 Web 静态文件与 Worker。

### 本地启动

```sh
nvm use 20
npm install
npm run dev
```

`npm run dev` 会同时启动 API 和 Web：API 默认监听 `9934` 端口，Web 开发服务器默认监听 `9935` 端口并代理 `/api`。首次启动后访问 `http://localhost:9935/setup` 创建超级管理员。默认 SQLite 文件保存到 `data/database/flycloud-helper.db`。

前端业务页面依赖后台 API；开发时可单独执行 `npm run dev:web` 启动界面进程，但需要同时存在可由 Vite 代理访问的后台。若只启动后台 API，可执行 `npm run dev:api`。TypeScript 静态检查命令：

```sh
npm run check
```

## 方案文档

- [Spec规格文档](doc/FlyCloudHelper远端媒体目录服务/Spec规格文档.md)
- [开发计划文档](doc/FlyCloudHelper远端媒体目录服务/开发计划文档.md)
- [需求与接口对照文档](doc/FlyCloudHelper远端媒体目录服务/需求与接口对照文档.md)
- [前端页面功能文档](doc/FlyCloudHelper远端媒体目录服务/前端页面功能文档.md)
- [后台接口与Docker部署说明](doc/FlyCloudHelper远端媒体目录服务/后台接口与Docker部署说明.md)
- [涉及系统与修改任务梳理](doc/需求任务梳理/FlyCloudHelper远端媒体目录服务涉及系统与修改任务梳理.md)
